---
# GitHub Action for Level 1 auditing of CVEs
# It is triggered whenever the CVE Update GitHub Action completes

name: CVE Auditor
on:
  workflow_run:
    workflows:
      - "CVE Update"
    types:
      - completed

jobs:
  audit-cves:
    name: Test Step
    environment: deployment
    runs-on: ubuntu-latest
    env:
      CVE_DELTALOG_JSON: https://raw.githubusercontent.com/CVEProject/cvelistV5/main/cves/delta.json
    steps:
      # - name: Checkout repository
      #   uses: actions/checkout@v4
      #   with:
      #     fetch-depth: 1
      - name: Setup Node to specific version
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: check delta
        run: |
          pwd
          ls -al
          mkdir -p .github/workflows/dist
          curl -L -o .github/workflows/dist/index.js https://raw.githubusercontent.com/hkong/cvelistV5/refs/heads/main/.github/workflows/dist/index.js
          curl -L -o ./delta.json ${CVE_DELTALOG_JSON}
          ls -al
          head ./delta.json
          node ./.github/workflows/dist/index.js --version
